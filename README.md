# S3 Minio Integration sample project for NET

## Start docker container for S3 Minio
```bash
cd /docker
./start.sh
```

## Stop and Remove docker container for S3 Minio
```bash
cd /docker
./down.sh
```
